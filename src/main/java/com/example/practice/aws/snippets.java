package com.example.practice.aws;

import java.util.Arrays;
import java.util.Comparator;
import java.util.List;
import java.util.stream.Collectors;

public class snippets {
    //Shortest distance matrix : https://www.geeksforgeeks.org/shortest-distance-two-cells-matrix-grid/
    // array to List
 // List<Integer> sortArray = Arrays.stream(new int[]{1,2,3}).boxed().distinct().count() ==
    //arrayTolist reversed order
   // List<Integer> sortArray = Arrays.stream(arr).boxed().sorted(Comparator.reverseOrder()).collect(Collectors.toList());
    // IntStream.rangeClosed(


}
